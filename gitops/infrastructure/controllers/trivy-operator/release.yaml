apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trivy-operator
  namespace: trivy-system
spec:
  interval: 30m
  chart:
    spec:
      chart: trivy-operator
      version: 0.33.1 # using a recent stable version
      sourceRef:
        kind: HelmRepository
        name: aqua
        namespace: trivy-system
      interval: 12h
  install:
    createNamespace: true
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  values:
    trivy:
      ignoreUnfixed: false
      storageClassEnabled: true
      storageClassName: proxmox-csi
      # Limit scan job resources to increase stability/memory limits (Trivy scan job container resources)
      resources:
        requests:
          cpu: 250m
          memory: 512Mi
        limits:
          cpu: "1"
          memory: 2Gi
    # Alternative naming pattern for scanJob resources depending on exact chart version
    scanJob:
      resources:
        requests:
          cpu: 250m
          memory: 512Mi
        limits:
          cpu: "1"
          memory: 2Gi
    # Continuously scan workloads
    operator:
      vulnerabilityScannerEnabled: true
      configAuditScannerEnabled: false # Disabled to minimize resource consumption and avoid OOMs
      rbacAssessmentScannerEnabled: false # Disabled to minimize resource consumption and avoid OOMs
      exposedSecretScannerEnabled: false # Disable secret scanning to reduce time & cache lock errors
      vulnerabilityScannerScanOnlyCurrentRevisions: true
      # Continuous monitoring - rescan on schedule
      scanJobTimeout: 5m
      builtInTrivyServer: true # Enable built-in server
      # Reduce scan concurrency to exactly 1 to avoid resource contention and cache locks on single node
      scanJobsConcurrentLimit: 1
      concurrentScanJobsLimit: 1
    targetNamespaces: "" # Scan all namespaces
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
