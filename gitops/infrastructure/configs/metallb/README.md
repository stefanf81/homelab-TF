# MetalLB IP range

This is currently a placeholder example.

Before using this in a real cluster, update `ipaddresspool.yaml` to match an IP
range that is outside your DHCP scope and valid for your network.
