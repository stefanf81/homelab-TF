apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: taskflow
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: taskflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          # Redis 8.10 — major upgrade from 7.4. Redis 8 integrates previously
          # separate modules (search, JSON, time series, probabilistic) into the
          # core binary, but we use the standard redis-server CMD (not redis-full.conf),
          # so none of those modules load and the cache behavior is identical to 7.x.
          # License: RSALv2 / SSPLv1 / AGPLv3 (tri-licensed).
          image: redis:8.10.1-alpine
          imagePullPolicy: IfNotPresent
          args:
            # Cap memory to leave headroom for Redis overhead; evict LRU keys on pressure.
            - "--maxmemory"
            - "384mb"
            - "--maxmemory-policy"
            - "allkeys-lru"
            # Explicitly disable RDB snapshots AND AOF: /data is an emptyDir, so
            # persistence would not survive a Pod replacement. Redis's compiled
            # defaults otherwise enable RDB (save 3600 1 / 300 100 / 60 10000) and
            # stop-writes-on-bgsave-error, causing pointless fork/COW memory spikes
            # in this 512Mi container and turning transient bgsave failures into
            # write errors (MISCONF). Re-add only together with a real PVC.
            - "--save"
            - ""
            - "--appendonly"
            - "no"
            # Evict in a background thread (no main-thread stall) and sample more keys for accuracy.
            - "--lazyfree-lazy-eviction"
            - "yes"
            - "--maxmemory-samples"
            - "10"
            # Redis 8 I/O threading — offloads socket writes and protocol parsing to
            # 2 background threads. Reads remain on the main thread unless
            # --io-threads-do-reads is enabled; with 500m CPU this still helps
            # during eviction bursts. Reduce to 1 if CPU throttling is observed.
            - "--io-threads"
            - "2"
            # Guard against client-connection memory blowup. Caps the combined output
            # buffers of all connected clients (Lettuce + redis_exporter) at 5% of
            # maxmemory (~19 MB here). Exceeding the cap drops the greediest connection
            # rather than OOM-killing the pod.
            - "--maxmemory-clients"
            - "5%"
            # Expiry-scan CPU budget. 1 = default (balanced). Lower reduces CPU stolen
            # from the backend at the cost of stale keys lingering slightly longer.
            # Raise to 4 only if TTL-based key buildup is observed.
            - "--active-expire-effort"
            - "1"
            - "--requirepass"
            - "$(REDIS_PASSWORD)"
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: REDIS_PASSWORD
            - name: REDISCLI_AUTH
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: REDIS_PASSWORD
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 6379
              name: redis
          resources:
            # Burstable QoS by design: CPU requests == limits so Redis is never CPU-throttled,
            # while the 256Mi memory request can burst to the 512Mi RSS cap. A throttled L2
            # cache would add latency spikes that stall the backend's hot path.
            limits:
              cpu: "500m"
              memory: 512Mi
            requests:
              cpu: "500m"
              memory: 256Mi
          volumeMounts:
            - name: redis-storage
              mountPath: /data
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            periodSeconds: 15
            failureThreshold: 3
          startupProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 2
            periodSeconds: 3
            timeoutSeconds: 3
            failureThreshold: 10
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            periodSeconds: 10
            failureThreshold: 2
      volumes:
        - name: redis-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: taskflow
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: redis
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restrict-redis-access
  namespace: taskflow
spec:
  podSelector:
    matchLabels:
      app: redis
  policyTypes:
    - Ingress
  ingress:
    # Allow ingress to Redis ONLY from the backend and the Redis exporter.
    # Effective only because default-deny-datastore-ingress.yaml denies all other
    # ingress to this pod first.
    - from:
        - podSelector:
            matchLabels:
              app: taskflow-backend
        - podSelector:
            matchLabels:
              app: redis-exporter
      ports:
        - protocol: TCP
          port: 6379
