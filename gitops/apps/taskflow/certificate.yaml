apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: taskflow-jokerlab-cert
  namespace: taskflow
spec:
  secretName: taskflow-tls-secret
  dnsNames:
    - "jokerlab.dev"
    - "www.jokerlab.dev"
  issuerRef:
    name: letsencrypt-http01-production
    kind: ClusterIssuer
